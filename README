Thumbafon as a few elements that can be useful in different projects. It has a synthesis layer that uses additive
synthesis to create anti aliased wave forms.  It has an ObjC Audio Queue class.  It has a C socket and packing
methodology for streaming osc messages. And it has a "slippery button" class that allows sliding from one UIButton 
to another.